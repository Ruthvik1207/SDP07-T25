{
  "name": "sdp07-t25-universal-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && chmod +x ./node_modules/.bin/react-scripts && npm run build",
    "build:vercel": "cd frontend && npm install && chmod +x ./node_modules/.bin/react-scripts && npm run build",
    "install": "cd frontend && npm install",
    "start": "cd frontend && npm start"
  }
}
